import { createServerApiClient } from '../lib/api-client'; async function d() { const api = createServerApiClient(); const res = await api.get('/events?eventCode=B11F99'); console.log('Natural Lookup Result:', JSON.stringify(res, null, 2)); const res2 = await api.get('/events?type=VOTING'); console.log('All Voting Events (Natural):', (res2.data || res2.events || []).map((e: any) => e.eventCode).join(', ')); } d();
